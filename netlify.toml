[build]
  publish = "public"
  functions = "netlify/functions"
  command = "mkdir -p public && cp -r static public/"

[build.environment]
  PYTHON_VERSION = "3.12"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true
